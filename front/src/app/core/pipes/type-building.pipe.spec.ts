import { TypeBuildingPipe } from './type-building.pipe';

describe('TypeBuildingPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeBuildingPipe();
    expect(pipe).toBeTruthy();
  });
});
